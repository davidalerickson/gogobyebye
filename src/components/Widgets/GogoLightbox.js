import React from 'react'
import styled from "styled-components";

const Wrapper = styled.div`

`

const GogoLightbox = ({visible, image}) => {
    return (
        <Wrapper>
            This is styled Lightbox
        </Wrapper>
    )
}

export default GogoLightbox
